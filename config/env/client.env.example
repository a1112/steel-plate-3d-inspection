# Vite development proxy target and optional Tauri service origin.
# Browser builds call /api on the current page origin by default; this value is
# used by Vite's reverse proxy and only becomes a browser fallback after the
# same-origin request fails and the user confirms an explicit address.
VITE_INSPECTION_SERVICE_ORIGIN=http://127.0.0.1:4873
