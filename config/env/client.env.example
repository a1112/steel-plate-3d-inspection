# Web/Tauri client points to the independently running Rust service.
# Vite development proxy target and optional Tauri service origin.
# Production browser builds always use the current page origin (HTTP or HTTPS).
VITE_INSPECTION_SERVICE_ORIGIN=http://127.0.0.1:4873
