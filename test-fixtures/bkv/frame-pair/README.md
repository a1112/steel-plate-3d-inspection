# Minimal BKV frame-pair fixture

This fixture retains one 2D JPG and one matching 3D NPZ frame from camera 2,
frame 0000. It is suitable for lightweight parser and rendering tests, but it is
not a complete six-camera replay dataset. Use `scripts/fetch_sample_data.py`
for the full versioned sample.
